# <X:signature>
mos_voting_on_<X:signature> = {
	is_shown = {
		trigger_if = {
			limit = {
				MOS_this_is_legislature_owner = yes
			}
			has_variable = MOS_start_law_vote_<X:signature>
		}
		trigger_else = {
			any_liege_or_above = {
				MOS_i_am_legislature_owner_of = { POTENTIAL_UNDERLING = prev }
				has_variable = MOS_start_law_vote_<X:signature>
			}
		}
	}
}
<X:lawOptions>